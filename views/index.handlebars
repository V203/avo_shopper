


<div class="container-fluid">
    <div class="row">
        <div class="col-2"></div>
        <div class="col-8">
            <h4>Best deals we have for you</h4>
        </div>
        <div class="col-2"></div>

    </div>
</div>




<div class="container">
    <div class="row">
        <div class="col-12">
            <table class="table table-borderless table-dark">
                <tr>
                    <th>QTY</th>
                    <th>FOR</th>
                    <th>PRICE</th>
                </tr>

                {{#each bstDeals}}
                <strong>
                    <tr>
                    <td> {{this.qty}}</td>
                    <td>For Only </td>
                   <td> {{this.price}}</td>
                   </tr>
                </strong>
                {{/each}}
            </table>
        </div>
    </div>
</div>